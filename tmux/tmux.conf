unbind r
bind r source-file ~/.config/tmux/tmux.conf

# set -g prefix C-w
set -g prefix M-w
bind M-w send-prefix
set -s escape-time 0

set -g mouse on # Enable mouse mode

# Pane navigation with hjkl
# bind-key h select-pane -L
# bind-key j select-pane -D
# bind-key k select-pane -U
# bind-key l select-pane -R

# Smart pane switching with awareness of vim splits and windows
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"


# search sessions
bind s display-popup -w 40% -h 40% -T " #[fg=#eb6f92,bold] #S " -E '
tmux list-sessions -F "#{?session_attached,,#{session_name}}" |
sed "/^$/d" |
fzf --reverse \
  --no-preview |
while IFS= read -r s; do
  tmux switch-client -t "$s"
done'

# search windows in current session
# bind a display-popup -E "\
#     tmux list-windows -F '#{window_index} #{window_name}' |\
#     sed '/^$/d' |\
#     fzf --reverse --header jump-to-window |\
#     cut -d ' ' -f 1 |\
#     xargs tmux select-window -t"

# Resize active pane by +10 or -10 (auto-detects horizontal vs vertical split)
unbind [
unbind ]
bind [ run-shell 'W=$(tmux display -p "#{window_width}"); PW=$(tmux display -p "#{pane_width}"); if [ "$PW" -lt "$W" ]; then tmux resize-pane -x $(( $(tmux display -p "#{pane_width}") + 20 )); else tmux resize-pane -y $(( $(tmux display -p "#{pane_height}") + 10 )); fi'
bind ] run-shell 'W=$(tmux display -p "#{window_width}"); PW=$(tmux display -p "#{pane_width}"); if [ "$PW" -lt "$W" ]; then tmux resize-pane -x $(( $(tmux display -p "#{pane_width}") - 20 )); else tmux resize-pane -y $(( $(tmux display -p "#{pane_height}") - 10 )); fi'

# Set new panes to open in current directory or fallback to $HOME
bind c new-window -c "#{pane_current_path}"
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
set -g default-terminal "wezterm"
set -g focus-events on
set -ga terminal-overrides ",wezterm:Tc"
set -as terminal-features ",wezterm:usstyle"
set -as terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[4 q'
set -as terminal-overrides ',*:Smulx=\E[4\072%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58\0722\072\072%p1%{65536}%/%d\072%p1%{256}%/%{255}%&%d\072%p1%{255}%&%d%;m'

set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
set -g popup-style 'bg=#0e0e16'
set -g popup-border-style 'fg=#6b3858,bg=#0e0e16'
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g detach-on-destroy off     # switch to another session when current one is destroyed
set -g set-clipboard off         # don't use OSC 52; mouse clicks won't clobber clipboard
set -g allow-passthrough on      # allow terminal escape sequences through tmux

set-option -g status-position top

set -g 'status-format[1]' ''
set -g status 2

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v1.0.3'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'gufranco/tmux-tiling-revamped'

# set -g @catppuccin_status_background "#0f0f0f"  # carbonfox background
# set -g @catppuccin_status_background "#111019"    # catppuccin (mocha, custom base) background
set -g @catppuccin_status_background "#0e0e16"    # lake dweller background

set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_number_position "right"

set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W "

set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W "

set -g @catppuccin_status_left_separator " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"

set -g @catppuccin_directory_text "#{pane_current_path:-$HOME}"

# Run catppuccin plugin manually or through tpm
# ...
# Status modules config
set -g @catppuccin_date_time_text "%d-%m %I:%M"

setw -g status-left-length 100
setw -g status-right-length 100

set -g status-left ""

set -g status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_application}"
# set -ag status-right "#{E:@catppuccin_status_date_time}"

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Load Catppuccin theme
run '~/.tmux/plugins/catppuccin/tmux/catppuccin.tmux'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
